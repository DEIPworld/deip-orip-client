export * from './reviewsListStore';
export * from './reviewDetailsStore';

const STATE = {
  data: []
};

const GETTERS = {};

const ACTIONS = {};

const MUTATIONS = {};

export const reviewsStore = {
  state: STATE,
  getters: GETTERS,
  actions: ACTIONS,
  mutations: MUTATIONS,
  namespaced: true
};
